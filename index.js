const logger = require('./logger')
console.log('creciendo como desarrollador')
try {
    const div= (valor=>{return valor/2})
    const a=numero
    console.log(div(a))
   } catch (e) { 
        console.log(`Error del tipo: ${e}`)
        }
